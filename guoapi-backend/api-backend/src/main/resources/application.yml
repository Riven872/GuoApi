server:
  port: 9528
  servlet:
    context-path: /api
spring:
  profiles:
    # 默认激活本地环境
    active: local
  mvc:
    pathmatch:
      matching-strategy: ANT_PATH_MATCHER
  # session 失效时间（分钟）
  session:
    timeout: 86400
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: false
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      logic-delete-field: isDelete # 全局逻辑删除的实体字段名(since 3.3.0,配置后可以忽略不配置步骤2)
      logic-delete-value: 1 # 逻辑已删除值(默认为 1)
      logic-not-delete-value: 0 # 逻辑未删除值(默认为 0)

# 自定义接口签名 starter
guoapi:
  client:
    access-key: 4fdf837a8641f89ae9908fe49737ebf6
    secret-key: 29061a1f0f18d311b81cb96a29d379db

